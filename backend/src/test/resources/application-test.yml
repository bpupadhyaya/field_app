spring:
  datasource:
    url: jdbc:h2:mem:field_app_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test_secret_test_secret_test_secret_test_secret_123456
    expiration-minutes: 60
  cors:
    allowed-origins: http://localhost:5174
